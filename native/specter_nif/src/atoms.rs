rustler::atoms! {
    ok,
    error,

    lock_fail,

    // config
    ice_servers,
    invalid_configuration,

    not_found,
    webrtc_error,

    // send
    peer_connection_error,
    peer_connection_ready,
}
